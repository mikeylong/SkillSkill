# Prompt

Primary request:

> Use `/SkillSkill` to critique the `frontend-skill` in `before/SKILL.md` and rewrite the weak parts.

Follow-up request:

> Continue by slimming the core skill if needed so the result stays lean and callable.

Expected outcome:

- a critique grounded in routing, contract clarity, edge cases, output shape, and packaging quality
- a rewritten skill snapshot under `after/`
- a self-contained example that does not depend on the live external `~/.codex/skills/frontend-skill` install
